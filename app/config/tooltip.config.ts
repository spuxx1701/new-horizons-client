export const tooltipConfig = {
  // The amount of miliseconds the tooltip will wait until it closes.
  tooltipCloseDelay: 500,
  // The amount of miliseconds the tooltip takes to hide.
  tooltipHideTime: 200,
};
